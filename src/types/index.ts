export interface NavLink { label: string; path: string }
export interface StatItem { label: string; value: string | number }
export interface EventItem { id: string; title: string; date: string; description: string }
